{
  "name": "dbe-app",
  "version": "1.0.0",
  "private": true,
  "description": "Dutch Bitcoin Embassy — Community App (PWA)",
  "scripts": {
    "dev": "npx wrangler pages dev . --kv AUTH_KV",
    "deploy": "npx wrangler pages deploy ."
  },
  "dependencies": {
    "@noble/curves": "^1.4.0",
    "@scure/base": "^1.1.0",
    "@supabase/supabase-js": "^2.45.0"
  }
}
